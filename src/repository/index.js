const OperationRepository = require("./operation");
const UserRepository = require("./user");
const CategoryRepository = require("./category");

module.exports = { OperationRepository, UserRepository, CategoryRepository };
